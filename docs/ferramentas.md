# Ferramentas

Olist Tiny ERP expõe 1 ferramenta (todas somente leitura).

### 1. `search_tools`
**Input**: `action` (opcional), `query` (opcional), `min_score` (opcional), `force_reindex` (opcional), `queries` (opcional), `tool_id` (opcional), `arguments` (opcional)

Single entrypoint for MCP catalog.

## Prompts de exemplo

```
Liste os pedidos de venda dos últimos 7 dias
Qual o estoque atual do produto X?
Quais contas a receber estão em aberto este mês?
```
